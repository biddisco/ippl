include:
  - local: "ci/cscs/common.yml"

variables:
  CUDA12_9_UENV: "/capstor/store/cscs/cscs/public/uenvs/opal-x-gh200-mpich-gcc-2025-12-19-cuda-12.9.1.squashfs"

# ---------------------------------------------------------
# cuda build on daint gh200 with debug flags
# this uses a uenv loaded by the uenv-runner and then cmake invoked to build
# ---------------------------------------------------------
ippl-build-cuda12-sm90-debug:
  stage: ippl_build
  image: "${CUDA12_9_UENV}"
  extends: [.uenv-runner-daint-gh200]
  variables:
    PERSIST_IMAGE_NAME: ${CSCS_REGISTRY_PATH}/ippl_cuda12_debug:${CI_COMMIT_SHORT_SHA}
    WITH_UENV_VIEW: "default"
    BUILD_DIR: "ippl-build-debug"
    SLURM_JOB_NUM_NODES: 1
  before_script:
    # NOTE: error 'script config should be a string or a nested array of
    # strings up to 10 levels deep' -> add '' around the command to execute
    - echo "CI_PROJECT_URL=$CI_PROJECT_URL"
    - echo "CI_COMMIT_SHORT_SHA=$CI_COMMIT_SHORT_SHA"
    - "uenv_dir=`echo $UENV_VIEW |cut -d: -f1`"
    - "uenv_view=`echo $UENV_VIEW |cut -d: -f3`"
    - echo "UENV_VIEW=$UENV_VIEW uenv_dir=$uenv_dir uenv_view=$uenv_view"
  script:
    - mkdir $BUILD_DIR
    - cd $BUILD_DIR
    - >-
      cmake -S .. -B . -G Ninja 
      --preset alps-gh200
      -DCMAKE_BUILD_TYPE=Debug
      -DKokkos_VERSION=git.4.7.02
      -DKokkos_ARCH_HOPPER90=ON
      -DIPPL_SKIP_FAILING_TESTS=ON
      -DMPIEXEC_EXECUTABLE=/usr/bin/srun
      -DMPIEXEC_PREFLAGS="--uenv=${CUDA12_9_UENV};--view=${WITH_UENV_VIEW};$WRAPPER"
      -DMPIEXEC_MAX_NUMPROCS=4
    - ninja all
    - echo "Build directory size:"
    - du -sh .
    - find . -name \*.o -delete
    - echo "Cleanup directory size:"
    - du -sh .
  artifacts:
    paths:
      - $BUILD_DIR
    expire_in: 1 day

# ---------------------------------------------------------
# cuda build on daint gh200 with debug flags
# ---------------------------------------------------------
ippl-build-cuda12-sm90-release:
  stage: ippl_build
  image: "${CUDA12_9_UENV}"
  extends: [.uenv-runner-daint-gh200]
  variables:
    PERSIST_IMAGE_NAME: ${CSCS_REGISTRY_PATH}/ippl_cuda12_release:${CI_COMMIT_SHORT_SHA}
    WITH_UENV_VIEW: "default"
    BUILD_DIR: "ippl-build-release"
    SLURM_JOB_NUM_NODES: 1
  before_script:
    # NOTE: error 'script config should be a string or a nested array of
    # strings up to 10 levels deep' -> add '' around the command to execute
    - echo "CI_PROJECT_URL=$CI_PROJECT_URL"
    - echo "CI_COMMIT_SHORT_SHA=$CI_COMMIT_SHORT_SHA"
    - "uenv_dir=`echo $UENV_VIEW |cut -d: -f1`"
    - "uenv_view=`echo $UENV_VIEW |cut -d: -f3`"
    - echo "UENV_VIEW=$UENV_VIEW uenv_dir=$uenv_dir uenv_view=$uenv_view"
  script:
    - mkdir $BUILD_DIR
    - cd $BUILD_DIR
    - >-
      cmake -S .. -B . -G Ninja 
      --preset alps-gh200
      -DCMAKE_BUILD_TYPE=Release
      -DKokkos_VERSION=git.4.7.02
      -DKokkos_ARCH_HOPPER90=ON
      -DIPPL_SKIP_FAILING_TESTS=ON
      -DMPIEXEC_EXECUTABLE=/usr/bin/srun
      -DMPIEXEC_PREFLAGS="--uenv=${CUDA12_9_UENV};--view=${WITH_UENV_VIEW};$WRAPPER"
      -DMPIEXEC_MAX_NUMPROCS=4
    - ninja all
    - echo "Build directory size:"
    - du -sh .
    - find . -name \*.o -delete
    - echo "Cleanup directory size:"
    - du -sh .
  artifacts:
    paths:
      - $BUILD_DIR
    expire_in: 1 day
